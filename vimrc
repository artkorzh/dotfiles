"To get help about any option, use :help option
set termencoding=utf-8
set fileencodings=utf-8,cp1251 "Try to determine file encoding
set encoding=utf-8
set hlsearch "Highlight search pattern in text
set ignorecase "Ignore case in search
set smartcase "/The would find only 'The', while /the would find 'the' or 'The'
set incsearch "Instant search (search while typing a pattern)
set showmatch
set bs=2 "Make another behavior of backspace. See :help bs
set ruler "Show line and column number in right bottom corner
set autoindent "Keep indentation of previous line
set expandtab "Insert space characters instead of tabs
set tabstop=4 "How much spaces tab match
set shiftwidth=4
set softtabstop=4 "Backspace will delete multiple spaces by tabwith slices
filetype indent on "Turn on autoindent based in programming language. Determine by filetype
set pastetoggle=<F10> "To paste without cascade indentation
set showcmd "Show command (eg. d)
colorscheme peachpuff
set directory=~/.vim/swp "Directory for store .swp files

